yaml-cpp is a YAML parser and emitter in C++, written around the YAML 1.2 spec. It's a library that makes it easy to read and write YAML files in your C++ code. With yaml-cpp, you can parse YAML files into a structure that you can use in your program, and you can also write data back out to a YAML file. 